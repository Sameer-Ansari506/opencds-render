services:
  - type: web
    name: opencds-server
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /opencds/
    envVars:
      - key: JAVA_OPTS
        value: -Xmx512m -Xms256m

